ListViewMode = function (diary) {
    var modeKey = "list-mode";
    var diary = diary;

    function setup() {
    }

    function init() {
    }

    return {
        key: modeKey,
        setup: setup,
        init: init,
    }
};